---
import { getAllBlogPosts } from '../lib/notion/client.ts'
import {
  getPostLink,
  getDateStr,
  getStaticFilePath,
} from '../lib/blog-helpers.ts'
import { getPlatform, platformConfig, isNew } from '../lib/helpers/platform'
import Layout from '../layouts/Layout.astro'
import PostFeaturedImage from '../components/PostFeaturedImage.astro'
import HeroSection from '../components/HeroSection.astro'

const allPosts = await getAllBlogPosts()

// プロフィール画像（public/images/profile-icon.webp に配置）
const profileIconURL = getStaticFilePath('/images/profile-icon.webp')

// プラットフォーム別に記事を分類
const blogPosts = allPosts.filter((post) => !post.ExternalLink)
const notePosts = allPosts.filter((post) =>
  post.ExternalLink?.includes('note.com')
)
const zennPosts = allPosts.filter((post) =>
  post.ExternalLink?.includes('zenn.dev')
)

// 各プラットフォームのFeatured記事（最新1件）
const featuredBlog = blogPosts[0]
const featuredNote = notePosts[0]
const featuredZenn = zennPosts[0]

// Featured記事のSlug一覧
const featuredSlugs = [
  featuredBlog?.Slug,
  featuredNote?.Slug,
  featuredZenn?.Slug,
].filter(Boolean)

// 右側の記事リスト（Featuredを除く）
const rightPosts = allPosts
  .filter((post) => !featuredSlugs.includes(post.Slug))
  .slice(0, 8)

---

<!-- WebSite スキーマ (構造化データ) -->
<script
  type="application/ld+json"
  set:html={JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'WebSite',
    name: 'urapro.dev',
    url: Astro.site?.toString() || 'https://urapro.dev',
    description:
      'プログラマーあでぃによるWeb開発技術、プログラミングTIPS、読書記録を発信するブログ',
    author: {
      '@type': 'Person',
      name: 'あでぃ',
      url: 'https://x.com/tk_adio',
    },
    publisher: {
      '@type': 'Organization',
      name: 'urapro.dev',
    },
  })}
  slot="head-append"
/>

<Layout
  title="Home"
  path="/"
  description="urapro.dev – プログラマーUraによるWeb開発技術、プログラミングTIPS、読書記録を発信するブログ。実践的な情報であなたのスキルアップを応援します。"
  ogImage=""
>
  <HeroSection slot="hero" profileIconURL={profileIconURL} />

  <div slot="main" class="home-content">
    <section class="posts-section">
      <h2 class="section-heading">最新の記事</h2>

      <div class="posts-grid">
        {/* 左側: 各プラットフォームのFeatured記事 */}
        <div class="featured-column">
          {/* Blog Featured */}
          {
            featuredBlog && (
              <>
                <div
                  class="mobile-platform-header"
                  style={`--platform-color: ${platformConfig.blog.color}`}
                >
                  <span class="mobile-platform-icon">
                    {platformConfig.blog.icon}
                  </span>
                  <span class="mobile-platform-name">
                    {platformConfig.blog.label}
                  </span>
                </div>
                <a href={getPostLink(featuredBlog)} class="featured-card">
                  <div
                    class="platform-badge"
                    style={`--platform-color: ${platformConfig.blog.color}`}
                  >
                    <span class="platform-icon">
                      {platformConfig.blog.icon}
                    </span>
                    <span class="platform-label">
                      {platformConfig.blog.label}
                    </span>
                  </div>
                  {isNew(featuredBlog.Date) && <div class="new-badge">NEW</div>}
                  <div class="featured-image">
                    <PostFeaturedImage post={featuredBlog} enableLink={false} />
                  </div>
                  <div class="featured-content">
                    <h3 class="featured-title">
                      {featuredBlog.Icon?.Type === 'emoji' && (
                        <span class="post-icon">{featuredBlog.Icon.Emoji}</span>
                      )}
                      {featuredBlog.Title}
                    </h3>
                    <div class="featured-meta">
                      <span class="featured-tag">
                        {featuredBlog.Tags?.[0]?.name}
                      </span>
                      <span class="featured-date">
                        {getDateStr(featuredBlog.Date)}
                      </span>
                    </div>
                  </div>
                </a>
              </>
            )
          }

          {/* note Featured */}
          {
            featuredNote && (
              <>
                <div
                  class="mobile-platform-header"
                  style={`--platform-color: ${platformConfig.note.color}`}
                >
                  <span class="mobile-platform-icon">
                    {platformConfig.note.icon}
                  </span>
                  <span class="mobile-platform-name">
                    {platformConfig.note.label}
                  </span>
                </div>
                <a
                  href={getPostLink(featuredNote)}
                  target="_blank"
                  class="featured-card"
                >
                  <div
                    class="platform-badge"
                    style={`--platform-color: ${platformConfig.note.color}`}
                  >
                    <span class="platform-icon">
                      {platformConfig.note.icon}
                    </span>
                    <span class="platform-label">
                      {platformConfig.note.label}
                    </span>
                  </div>
                  {isNew(featuredNote.Date) && <div class="new-badge">NEW</div>}
                  <div class="featured-image">
                    <PostFeaturedImage post={featuredNote} enableLink={false} />
                  </div>
                  <div class="featured-content">
                    <h3 class="featured-title">
                      {featuredNote.Icon?.Type === 'emoji' && (
                        <span class="post-icon">{featuredNote.Icon.Emoji}</span>
                      )}
                      {featuredNote.Title}
                    </h3>
                    <div class="featured-meta">
                      <span class="featured-tag">
                        {featuredNote.Tags?.[0]?.name}
                      </span>
                      <span class="featured-date">
                        {getDateStr(featuredNote.Date)}
                      </span>
                    </div>
                  </div>
                </a>
              </>
            )
          }

          {/* Zenn Featured */}
          {
            featuredZenn && (
              <>
                <div
                  class="mobile-platform-header"
                  style={`--platform-color: ${platformConfig.zenn.color}`}
                >
                  <span class="mobile-platform-icon">
                    {platformConfig.zenn.icon}
                  </span>
                  <span class="mobile-platform-name">
                    {platformConfig.zenn.label}
                  </span>
                </div>
                <a
                  href={getPostLink(featuredZenn)}
                  target="_blank"
                  class="featured-card"
                >
                  <div
                    class="platform-badge"
                    style={`--platform-color: ${platformConfig.zenn.color}`}
                  >
                    <span class="platform-icon">
                      {platformConfig.zenn.icon}
                    </span>
                    <span class="platform-label">
                      {platformConfig.zenn.label}
                    </span>
                  </div>
                  {isNew(featuredZenn.Date) && <div class="new-badge">NEW</div>}
                  <div class="featured-image">
                    <PostFeaturedImage post={featuredZenn} enableLink={false} />
                  </div>
                  <div class="featured-content">
                    <h3 class="featured-title">
                      {featuredZenn.Icon?.Type === 'emoji' && (
                        <span class="post-icon">{featuredZenn.Icon.Emoji}</span>
                      )}
                      {featuredZenn.Title}
                    </h3>
                    <div class="featured-meta">
                      <span class="featured-tag">
                        {featuredZenn.Tags?.[0]?.name}
                      </span>
                      <span class="featured-date">
                        {getDateStr(featuredZenn.Date)}
                      </span>
                    </div>
                  </div>
                </a>
              </>
            )
          }
        </div>

        {/* 右側: 全プラットフォーム混合リスト（サムネイル付き） */}
        <div class="list-column">
          <div class="list-header">
            <span class="list-header-title">More</span>
          </div>
          <ul class="posts-list">
            {
              rightPosts.map((post) => {
                const platform = getPlatform(post)
                const config = platformConfig[platform]
                return (
                  <li class="post-item">
                    <a
                      href={getPostLink(post)}
                      target={post.ExternalLink ? '_blank' : undefined}
                      class="post-item-link"
                    >
                      <div class="post-item-thumb">
                        <PostFeaturedImage post={post} enableLink={false} />
                      </div>
                      <span class="post-item-icon">
                        {post.Icon?.Type === 'emoji' ? post.Icon.Emoji : '📝'}
                      </span>
                      <span class="post-item-title">{post.Title}</span>
                      <span class="post-item-meta">
                        <span class="post-item-badges">
                          {isNew(post.Date) && (
                            <span class="new-badge-small">NEW</span>
                          )}
                          <span
                            class="post-item-platform"
                            style={`--platform-color: ${config.color}`}
                          >
                            {config.label}
                          </span>
                        </span>
                        <span class="post-item-date">
                          {getDateStr(post.Date)}
                        </span>
                      </span>
                    </a>
                  </li>
                )
              })
            }
          </ul>
        </div>
      </div>

      <div class="see-more-wrapper">
        <a href="/posts" class="see-more-button">すべての記事を見る →</a>
      </div>
    </section>
  </div>
</Layout>

<style>
  .home-content {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .posts-section {
    margin-bottom: 3rem;
  }

  .section-heading {
    font-size: 1.3rem;
    font-weight: 700;
    color: var(--fg);
    margin: 0 0 1.5rem;
    padding-left: 1rem;
    position: relative;
  }

  .section-heading::before {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 4px;
    height: 1.2em;
    background: linear-gradient(180deg, var(--accents-1) 0%, #d4693a 100%);
    border-radius: 2px;
  }

  /* 2カラムグリッド - 左:狭め 右:広め、高さ揃え */
  .posts-grid {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 1.5rem;
    align-items: stretch;
  }

  /* 左側: Featured カラム */
  .featured-column {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  /* 右側: リストカラム */
  .list-column {
    display: flex;
    flex-direction: column;
    background: var(--card-bg);
    border-radius: 12px;
    border: 1px solid var(--border-color);
    overflow: hidden;
  }

  /* Featured カード - 縦型で大きく表示、均等高さ */
  .featured-card {
    display: flex;
    flex-direction: column;
    flex: 1;
    background: var(--card-bg);
    border-radius: 12px;
    overflow: visible;
    text-decoration: none;
    color: inherit;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    border: 1px solid var(--border-color);
    position: relative;
  }

  /* カードホバー時のパルスリング */
  .featured-card::before {
    content: '';
    position: absolute;
    top: -4px;
    left: -4px;
    right: -4px;
    bottom: -4px;
    border-radius: 16px;
    border: 2px solid var(--platform-color, var(--accents-1));
    opacity: 0;
    transition: opacity 0.3s ease;
    pointer-events: none;
  }

  .featured-card:hover::before {
    animation: card-pulse 1.5s ease-out infinite;
  }

  @keyframes card-pulse {
    0% {
      opacity: 0.6;
      transform: scale(1);
    }
    100% {
      opacity: 0;
      transform: scale(1.03);
    }
  }

  .featured-card:hover {
    transform: translateY(-4px);
    box-shadow:
      0 8px 24px rgba(0, 0, 0, 0.1),
      0 0 0 2px var(--platform-color, var(--accents-1)),
      inset 0 0 0 1px rgba(255, 255, 255, 0.1);
    border-color: transparent;
  }

  .featured-card:hover .featured-image :global(img) {
    transform: scale(1.03);
  }

  /* プラットフォームバッジ - サムネイル上に表示 */
  .platform-badge {
    position: absolute;
    top: 0.5rem;
    left: 0.5rem;
    z-index: 10;
    display: flex;
    align-items: center;
    gap: 0.25rem;
    padding: 0.3rem 0.5rem;
    background: var(--platform-color, #888);
    border-radius: 6px;
    color: #fff;
    font-size: 0.7rem;
    font-weight: 600;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
  }

  .platform-icon {
    font-size: 0.85rem;
  }

  .platform-label {
    display: inline;
  }

  /* NEW バッジ - カード用 */
  .new-badge {
    position: absolute;
    top: 0.5rem;
    right: 0.5rem;
    z-index: 10;
    padding: 0.25rem 0.5rem;
    background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
    border-radius: 4px;
    color: #fff;
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.05em;
    box-shadow: 0 2px 8px rgba(238, 90, 36, 0.4);
    animation: new-badge-pulse 2s ease-in-out infinite;
  }

  @keyframes new-badge-pulse {
    0%,
    100% {
      box-shadow: 0 2px 8px rgba(238, 90, 36, 0.4);
      transform: scale(1);
    }
    50% {
      box-shadow:
        0 2px 16px rgba(238, 90, 36, 0.7),
        0 0 20px rgba(255, 107, 107, 0.4);
      transform: scale(1.05);
    }
  }

  /* NEW バッジ - リスト用（小さめ） */
  .new-badge-small {
    padding: 0.1rem 0.3rem;
    background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
    border-radius: 3px;
    color: #fff;
    font-size: 0.5rem;
    font-weight: 700;
    letter-spacing: 0.03em;
    animation: new-badge-pulse-small 2s ease-in-out infinite;
  }

  @keyframes new-badge-pulse-small {
    0%,
    100% {
      box-shadow: none;
    }
    50% {
      box-shadow: 0 0 10px rgba(255, 107, 107, 0.5);
    }
  }

  .featured-image {
    width: 100%;
    aspect-ratio: 16 / 10;
    overflow: hidden;
    background: linear-gradient(135deg, #f5f0e8 0%, #ede5da 100%);
  }

  .featured-image :global(.post-featured-image) {
    aspect-ratio: 16 / 10;
    height: 100%;
  }

  .featured-image :global(img) {
    width: 100%;
    height: 100%;
    object-fit: contain;
    transition: transform 0.3s ease;
  }

  .featured-content {
    padding: 0.75rem;
  }

  .featured-title {
    margin: 0 0 0.4rem;
    font-size: 0.9rem;
    font-weight: 600;
    line-height: 1.4;
    color: var(--fg);
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .post-icon {
    margin-right: 0.15em;
  }

  .featured-meta {
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }

  .featured-tag {
    padding: 0.15rem 0.4rem;
    background: var(--hover-bg);
    border-radius: 3px;
    font-size: 0.65rem;
    color: var(--accents-3);
  }

  .featured-date {
    font-size: 0.7rem;
    color: var(--accents-2);
  }

  /* モバイル用プラットフォームヘッダー - PCでは非表示 */
  .mobile-platform-header {
    display: none;
  }

  /* シンプルリスト - 左側と高さを揃える */
  .posts-list {
    list-style: none;
    margin: 0;
    padding: 0;
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .post-item {
    flex: 1;
    display: flex;
    border-bottom: 1px solid var(--border-color);
    position: relative;
  }

  .post-item:last-child {
    border-bottom: none;
  }

  /* リストアイテムホバー時のパルスリング */
  .post-item::before {
    content: '';
    position: absolute;
    top: -2px;
    left: -2px;
    right: -2px;
    bottom: -2px;
    border-radius: 8px;
    border: 2px solid var(--accents-1);
    opacity: 0;
    transition: opacity 0.3s ease;
    pointer-events: none;
    z-index: 1;
  }

  .post-item:hover::before {
    animation: list-item-pulse 1.5s ease-out infinite;
  }

  @keyframes list-item-pulse {
    0% {
      opacity: 0.5;
      transform: scale(1);
    }
    100% {
      opacity: 0;
      transform: scale(1.01);
    }
  }

  .post-item-link {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.6rem 0.75rem;
    text-decoration: none;
    color: inherit;
    transition: background 0.2s ease;
    box-sizing: border-box;
    width: 100%;
    position: relative;
    z-index: 2;
  }

  .post-item-link:hover {
    background: var(--hover-bg);
  }

  .post-item-thumb {
    width: 120px;
    height: 68px;
    flex-shrink: 0;
    border-radius: 6px;
    overflow: hidden;
    background: linear-gradient(135deg, #f5f0e8 0%, #ede5da 100%);
  }

  .post-item-thumb :global(.post-featured-image) {
    width: 100%;
    height: 100%;
  }

  .post-item-thumb :global(img) {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .post-item-icon {
    font-size: 1rem;
    flex-shrink: 0;
    width: 1.25rem;
    text-align: center;
  }

  .post-item-title {
    flex-grow: 1;
    font-size: 0.875rem; /* PC: 14px (was 0.8rem/12.8px) - 少し大きく */
    font-weight: 500;
    color: var(--fg);
    line-height: 1.4;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .post-item-meta {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.15rem;
    flex-shrink: 0;
  }

  .post-item-platform {
    padding: 0.1rem 0.35rem;
    background: var(--platform-color, #888);
    border-radius: 3px;
    font-size: 0.55rem;
    font-weight: 600;
    color: #fff;
  }

  .post-item-date {
    font-size: 0.6rem;
    color: var(--accents-2);
    white-space: nowrap;
  }

  /* バッジ横並びラッパー */
  .post-item-badges {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  /* 一覧ヘッダー */
  .list-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 0.75rem;
    background: var(--hover-bg);
    border-bottom: 1px solid var(--border-color);
  }

  .list-header::before {
    content: '';
    width: 3px;
    height: 1em;
    background: linear-gradient(180deg, var(--accents-1) 0%, #d4693a 100%);
    border-radius: 1px;
  }

  .list-header-title {
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--accents-2);
    letter-spacing: 0.03em;
    text-transform: uppercase;
  }

  /* もっと見るボタン */
  .see-more-wrapper {
    margin-top: 2rem;
    text-align: center;
  }

  .see-more-button {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    padding: 0.75rem 2rem;
    background: linear-gradient(135deg, var(--accents-1) 0%, #d4693a 100%);
    border-radius: 9999px;
    color: #fff;
    font-size: 0.9rem;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 4px 12px rgba(224, 122, 61, 0.25);
  }

  .see-more-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 16px rgba(224, 122, 61, 0.35);
    color: #fff;
  }

  /* レスポンシブ */
  @media (max-width: 900px) {
    .posts-grid {
      grid-template-columns: 240px 1fr;
    }
  }

  @media (max-width: 768px) {
    .posts-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .featured-column {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 0.75rem;
    }

    .featured-image {
      aspect-ratio: 4 / 3;
    }

    .featured-image :global(.post-featured-image) {
      aspect-ratio: 4 / 3;
    }

    .featured-content {
      padding: 0.6rem;
    }

    .featured-title {
      font-size: 0.8rem;
      -webkit-line-clamp: 2;
    }

    .platform-badge {
      padding: 0.2rem 0.4rem;
      font-size: 0.6rem;
    }

    .platform-label {
      display: none;
    }
  }

  @media (max-width: 640px) {
    .home-content {
      padding: 0 1rem;
    }

    .featured-column {
      display: flex;
      flex-direction: column;
      gap: 0;
      background: var(--card-bg);
      border-radius: 12px;
      border: 1px solid var(--border-color);
      overflow: hidden;
    }

    /* モバイル用プラットフォームヘッダー */
    .mobile-platform-header {
      display: flex;
      align-items: center;
      gap: 0.35rem;
      padding: 0.5rem 0.75rem 0.25rem;
      background: var(--hover-bg);
      border-top: 1px solid var(--border-color);
    }

    .mobile-platform-header:first-child {
      border-top: none;
    }

    .mobile-platform-icon {
      font-size: 0.85rem;
    }

    .mobile-platform-name {
      font-size: 0.7rem;
      font-weight: 600;
      color: var(--platform-color, var(--fg));
    }

    /* Featured記事を右側リストと同じ横長スタイルに */
    .featured-card {
      flex-direction: row;
      align-items: center;
      gap: 0.6rem;
      padding: 0.5rem 0.75rem 0.6rem;
      border-radius: 0;
      border: none;
    }

    .featured-card:hover {
      transform: none;
      box-shadow: none;
      background: var(--hover-bg);
    }

    .featured-image {
      width: 88px;
      height: 50px;
      aspect-ratio: auto;
      flex-shrink: 0;
      border-radius: 6px;
    }

    .featured-image :global(.post-featured-image) {
      aspect-ratio: auto;
      width: 100%;
      height: 100%;
    }

    .platform-badge {
      position: static;
      padding: 0.1rem 0.35rem;
      font-size: 0.55rem;
      margin: 0;
      order: 3;
      flex-shrink: 0;
    }

    /* NEWバッジをモバイルでプラットフォームバッジの隣に配置 */
    .new-badge {
      position: static;
      order: 4;
      padding: 0.1rem 0.3rem;
      font-size: 0.5rem;
      margin: 0;
      flex-shrink: 0;
    }

    .platform-label {
      display: inline;
    }

    .platform-icon {
      display: none;
    }

    .featured-content {
      flex-grow: 1;
      padding: 0;
      min-width: 0;
      display: flex;
      flex-direction: column;
      gap: 0.15rem;
    }

    .featured-title {
      font-size: 0.8rem;
      -webkit-line-clamp: 3; /* スマホ: 3行まで表示 */
      margin: 0;
    }

    .featured-meta {
      display: none;
    }

    /* リストのタイトルも3行まで表示 */
    .post-item-title {
      font-size: 0.8rem;
      -webkit-line-clamp: 3; /* スマホ: 3行まで表示 */
    }

    .post-item-link {
      padding: 0.5rem 0.6rem;
    }

    .post-item-thumb {
      width: 88px;
      height: 50px;
    }

    /* 一覧ヘッダーのモバイル調整 */
    .list-header {
      padding: 0.4rem 0.6rem;
    }

    .list-header-title {
      font-size: 0.7rem;
    }
  }
</style>
